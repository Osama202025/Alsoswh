name: The Behemoth Agent
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run_agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run The Core
        run: python agent_core.py
