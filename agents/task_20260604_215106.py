لنقوم بإنشاء ملف بايثون جديد يُدعى `welcome.py` داخل مجلد `agents/` وستحتوي على كود يطبع رسالة ترحيبية باسم إمبراطورية الوحش، يمكننا اتباع هذه الخطوات:

1. **إنشاء المجلد وملف بايثون**:
   - إذا لم يكن المجلد `agents/` موجودًا، قم بإنشائه باستخدام الأمر:
     ```bash
     mkdir agents
     ```
   - انقر نقراً مزدوجاً على المجلد `agents/` وقم بإنشاء ملف جديد باسم `welcome.py` внутри هذا المجلد. يمكنك استخدام محرر نصوص أو بيئة تطوير متكاملة (IDE) لإنشاء الملف.

2. **كتابة الكود في ملف `welcome.py`**:
   - افتح الملف `welcome.py` الذي تم إنشاؤه واكتب الكود التالي:
     ```python
     # الملف: agents/welcome.py

     def print_welcome_message():
         """طبع رسالة ترحيبية باسم إمبراطورية الوحش."""
         empire_name = "إمبراطورية الوحش"
         welcome_message = f"مرحباً بكم في {empire_name}!"
         print(welcome_message)

     if __name__ == "__main__":
         print_welcome_message()
     ```

3. **تشغيل الملف**:
   - بعد كتابة الكود، احفظ الملف.
   - افتح نافذة طرفية (terminal) أو موجه أوامر (command prompt) وانتقل إلى المجلد الذي يحتوي على مجلد `agents/`.
   - استخدم الأمر التالي لتشغيل الملف:
     ```bash
     python agents/welcome.py
     ```
   - يجب أن تظهر الرسالة التالية:
     ```
     مرحباً بكم في إمبراطورية الوحش!
     ```

بهذا تكون قد أنشأت بنجاح ملف `welcome.py` داخل مجلد `agents/` وكتابة كود بايثون يطبع رسالة ترحيبية باسم إمبراطورية الوحش.